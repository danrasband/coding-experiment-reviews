# you can write to stdout for debugging purposes, e.g.
# print("this is a debug message")

def solution(A):
    for i in range(100):
        prefix = ''
        pos_prefix = A[0][0]
        for word in A:
            if word[0] == pos_prefix
    pass